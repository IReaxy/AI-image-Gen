const API_Key = "" //put the key here
const submitIcon = document.querySelector("#submit-icon");

const getImages = () => {

        try {

            fetch( //pull URL here , options)
            }
            catch (error) {
                console.error(error)

            }


        }


        submitIcon.addEventListener('click', getImages)